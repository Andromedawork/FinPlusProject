var $select = $('#select-state').selectize({
    create                   : false,
    respect_word_boundaries: false,
    plugins: ['remove_button'],
});

var $select1 = $('#select-state-second').selectize({
    create: false,
    respect_word_boundaries: false,
    plugins: ['remove_button'],
});
var $select = $('#select-state-third').selectize({
    create: false,
    respect_word_boundaries: false,
    plugins: ['remove_button'],
});

var $select1 = $('#select-state-fourthy').selectize({
    create: false,
    respect_word_boundaries: false,
    plugins: ['remove_button'],
});
